# Memact Landing

Static landing page for Memact.

## Local preview

```powershell
cd C:\Users\sujay\Downloads\memact_landing
python -m http.server 8000 --bind 0.0.0.0
```

Open `http://localhost:8000` or your LAN IP on a phone.

## Render

`render.yaml` is configured for a static site. Deploy the repo root.


